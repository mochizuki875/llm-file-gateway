VLLM_MODEL=your-model-name
VLLM_BASE_URL=http://vllm-host:8000/v1
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=8080
GATEWAY_AUTH_REQUIRED=false
GATEWAY_API_KEY=your-gateway-api-key
VLLM_API_KEY=your-vllm-api-key
GATEWAY_DATA_DIR=./gateway-data
# ファイル保持期間(sec)とexpires_after.secondsの上限（0で無制限）
FILE_TTL_SECONDS=300
# 1ファイルの最大サイズ(bytes)（0で無制限）
MAX_FILE_BYTES=52428800
# 推論リクエストボディ全体の最大サイズ(bytes)。未設定時はMAX_FILE_BYTESの4倍（0で無制限）
MAX_REQUEST_BODY_BYTES=209715200
# 画像変換する最大ページ数（0で無制限）。変換とvLLMへの送信の両方に適用
MAX_DOCUMENT_PAGES=50
# 1ファイルから抽出するテキストの最大文字数（0で無制限）
MAX_DOCUMENT_TEXT_CHARS=500000
DOCUMENT_DPI=300
DOCUMENT_TEXT_EXTRACTION_ENABLED=true
CONVERSION_WORKERS=2
# vLLM通信のtimeout(sec)。streamingではstream全体に適用（0で無制限）
REQUEST_TIMEOUT_SECONDS=300
LOGLEVEL=0